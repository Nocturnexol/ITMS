@model Bitshare.DataDecision.Model.OperationalAuthority

@{
    ViewBag.Title = "业务权限-添加";
    Layout = "~/Views/Shared/_OperateNew.cshtml";
}
<div class="form-group form-groupm" style="margin-top:10px">
    <label class="col-sm-2 control-label">
        业务名称<span style="color: #FF9966"> *</span></label>
    <div class="col-sm-3">
        @Html.TextBoxFor(modelItem => modelItem.OperRational_Name, new { @placeholder = "业务名称", @class = "form-control", @required = "" })
    </div>
    <label class="col-sm-2 control-label">
        权限描述<span style="color: #FF9966"> *</span></label>
    <div class="col-sm-3">
        <select multiple="multiple" name="RightsOptions" id="RightsOptions" class="chosen-select" data-placeholder = "请选择节点"  style="width: 132px">
            <option>查看本人</option>
            <option>查看下级</option>
            <option>查看所有</option>
            <option>查看本部门</option>
        </select>
    </div>
    </div>
<div class="form-group form-groupm" style="margin-top:10px">
@*    <label class="col-sm-2 control-label">
        选择方式<span style="color: #FF9966"> *</span></label>
    <div class="col-sm-3">
        <select name="Options" class="form-control">
            <option>单选</option>
            <option>多选</option>
        </select>
    </div>*@
    
    <label class="col-sm-2 control-label">
        备注</label>
    <div class="col-sm-8">
        @Html.TextBoxFor(modelItem => modelItem.Remark, new { @placeholder = "备注", @class = "form-control" })
    </div>
</div>
